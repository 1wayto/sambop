# Sambop with OpenAI Codex CLI

Draft guide. Configure Sambop MCP when Codex MCP support is available in the target environment, or use generated repo instructions plus normal Git workflow.

Required behavior:

```text
read governance → branch → change → test → push → PR
```
